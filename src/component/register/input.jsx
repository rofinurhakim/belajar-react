



const InputRegis = (props) => {
    return(
        <input className="input" type={props.type} 
        name={props.name} 
        onChange={props.fungsi}
        value={props.value}>
        </input>
       
    )
    
}

export default InputRegis

