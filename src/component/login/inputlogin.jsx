


const InputLogin = (props) => {
    return(
        <input type={props.type} 
        name={props.name} 
        onChange={props.fungsi}
        value={props.value}>
        </input>
       
    )
    
}

export default InputLogin